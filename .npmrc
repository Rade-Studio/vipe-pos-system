# pnpm v11+ requires allowBuilds in .npmrc for build script permissions
# strictDepBuilds=true (default) errors on unreviewed build scripts
allowBuilds:
  sharp: true
  "@parcel/watcher": true
  esbuild: true
  unrs-resolver: true
