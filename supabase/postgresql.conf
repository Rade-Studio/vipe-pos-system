# PostgreSQL config override for Supabase self-hosted.
# Mounted to /etc/postgresql/postgresql.conf via docker-compose.
listen_addresses = '*'
wal_level = logical
max_replication_slots = 10
max_wal_senders = 10
shared_preload_libraries = 'pg_cron,pg_net,pg_stat_statements,supabase_vault'